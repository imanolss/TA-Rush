// Collect The Square game

// Get a reference to the canvas DOM element
const canvas = document.getElementById('canvas')
// Get the canvas drawing context
const context = canvas.getContext('2d')
//Canvas7
/*
function portada() {
  imgPortada = new Image()
  imgPortada.src = 'images/portada_game.png'
  imgPortada.onload = function() {
    context.drawImage(imgPortada, 0, 0)
  }
}
*/
window.onload = function() {
  let portada = document.getElementById('canvas')
  let ctx = portada.getContext('2d')
}
